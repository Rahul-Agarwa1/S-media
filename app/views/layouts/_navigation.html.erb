<nav class="navbar navbar-expand-lg navbar-light bg-light">
   <%= link_to "Socio Menta", root_path, class: "navbar-brand", id: "logo" %>
   <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
     <span class="navbar-toggler-icon"></span>
   </button>

   <div class="collapse navbar-collapse" id="navbarSupportedContent">
     <ul class="navbar-nav mr-auto">
       
       <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Friends
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "My friends", user_friends_path(current_user), class: "dropdown-item" %>
            <%= link_to "Pending Requests", user_receieved_request_path(current_user), class: "dropdown-item" %>
            <%= link_to "Requests Sent", user_pending_request_path(current_user), class: "dropdown-item" %>
          </div>
        </li>
       <% end %>
       <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Posts
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Create new Post", new_post_path, class: "dropdown-item" %>
            <%= link_to "View Posts", posts_path, class: "dropdown-item" %>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Something else here</a>
          </div>
        </li>
       <% end %>  
       <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Profile [<%= current_user.full_name %>]
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "View your profile", myprofile_path, class: "dropdown-item" %>
            <%= link_to "Edit your profile", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Delete profile", '/users', method: :delete ,class: "dropdown-item text-danger", data: { confirm: "Are you sure?" } %> 
            </div>
        </li>
         <li class="nav-item">
           <%= link_to 'Logout', destroy_user_session_path, class: "nav-link", method: :delete %>
         </li>
       <% else %>
         <li class="nav-item">
           <%= link_to 'Log in', signin_path, class: "nav-link" %>
         </li>
         <li class="nav-item">
           <%= link_to 'Sign up', signup_path, class: "nav-link" %>
         </li>
       <% end %>
       
     </ul>
     <%= form_tag search_friend_path,class: "form-inline my-2 my-lg-0", remote: true, method: :get do %>
       <%= text_field_tag :friend, params[:friend], placeholder: "Search" , 
       class: "form-control mr-sm-2" %>
       <%= button_tag type: :submit, class: "btn btn-outline-success my-2 my-sm-0" do %>
          Search
        <% end %>
     <% end %>

   </div>
 </nav>

 <div id="friend-results">

 </div>

